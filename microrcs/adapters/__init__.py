"""Benchmark adapters for microRCS.

Currently ships:
- `swe_bench` — SWE-bench-Lite long-horizon agent benchmark adapter (BRO-946).

Adapters consume the existing microRCS public surface (Task, Caps, Workspace,
L0Plant, EpisodeTrace) without modifying microrcs.py mainline.
"""
